version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --ignore-scripts
    build:
      commands:
        - npx nx build web
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .nx/cache/**/*
