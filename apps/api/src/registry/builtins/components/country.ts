import type { SelectPrimitive } from "../../types";

export const Country: SelectPrimitive = {
    fieldId: "country",
    label: "Country",
    htmlType: "select",
    options: [],
    multiple: false,
}
