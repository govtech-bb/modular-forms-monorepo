import type { SelectPrimitive } from "../../types";

export const AccountType: SelectPrimitive = {
    fieldId: "account-type",
    label: "Account type",
    htmlType: "select",
    options: [],
    multiple: false,
}
