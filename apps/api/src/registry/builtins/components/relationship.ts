import { SelectPrimitive } from "../../types";

export const Relationship: SelectPrimitive = {
  fieldId: "relationship",
  htmlType: "select",
  label: "Relationship",
  options: [],
  multiple: false,
}
