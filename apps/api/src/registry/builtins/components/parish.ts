import type { SelectPrimitive } from "../../types";

export const Parish: SelectPrimitive = {
    fieldId: "parish",
    label: "Parish",
    htmlType: "select",
    options: [],
    multiple: false,
}
